# Copy to .env and fill in. NEVER commit your real .env (it's in .gitignore).

# Teacher model for distillation (Night 1) + LLM-as-judge (Night 3)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Hugging Face Hub — needed Night 1 (dataset push) and Night 2 (adapter push)
# Create at https://huggingface.co/settings/tokens with "write" scope
HF_TOKEN=hf_your-token-here
